# edt

use `cmd+alt+p` to toggle quick open with the `edt ` inserted into it to list all active editors.
think of it as a replacement for tabs

use `cmd+ctrl+p` to toggle quick open with the path of the active editor inserted. this a replacement of breadcrumbs